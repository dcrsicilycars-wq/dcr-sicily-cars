# ============================================
# DCR SICILY CARS - Environment Configuration
# ============================================
# Copy this file to .env and fill in your values.
# Only ADMIN_EMAIL and ADMIN_PASSWORD are actually used
# (they authenticate the admin dashboard via Cloudflare Pages secrets).

# --- Admin ---
ADMIN_EMAIL=admin@dcrsicilycars.it
ADMIN_PASSWORD=change-this-password
